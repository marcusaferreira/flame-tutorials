name: space_shooter
description: "Space shooter Flame tutorial"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.2

dependencies:
  flame: ^1.28.1
  flutter:
    sdk: flutter
  web: ^1.1.0

dev_dependencies:
  flame_lint: ^1.3.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/images/
